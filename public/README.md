# Static assets served from the site root

This folder is required by Next.js / Vercel even when empty. Drop
favicons, OpenGraph images, fonts, and other static files here.

Currently intentionally empty for v0.1.
