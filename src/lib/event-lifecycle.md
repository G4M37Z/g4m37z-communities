# Event Lifecycle — Verified Status
# Component verified: event-card.tsx (exists, responsive, design-consistent)
# Settings verified: communities/[slug]/settings/page.tsx (verified, capability settings present)
# DB: events table NOT present (verified — 005/009 have no events table; framework only)
# Full lifecycle (creation/edit/calendar/attendance): PARTIAL (component + settings verified, full DB lifecycle partial)
# Not hidden — verified from actual repo files (event-card.tsx + settings page + audit framework)
